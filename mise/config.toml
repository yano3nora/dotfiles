[tools]
node = "24.14.1"
python = "3.13.7"
jq = "1.7.1"
ripgrep = "15.1.0"
deno = "1.40.5"
# php@8.1 is still managed by Homebrew for now. mise php requires build dependencies such as autoconf.

[settings]
idiomatic_version_file_enable_tools = ["node", "python"]

[settings.status]
