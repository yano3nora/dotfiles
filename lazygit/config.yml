os:
  editPreset: nvim

gui:
  mouseEvents: true

git:
  pagers:
    - colorArg: always
      # pager: delta --dark --paging=never --side-by-side --line-numbers
      pager: hunk pager --transparent-bg
