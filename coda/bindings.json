// User key bindings. JSONC comments are allowed.
// `command` uses Coda's internal action name, not a VS Code command ID.
[
  // { "key": "ctrl+j", "command": "cursor.down", "when": "editorFocus" }
  { "key": "ctrl+f", "command": "search.open" },
]
